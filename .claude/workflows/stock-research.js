export const meta = {
  name: 'stock-research',
  description: '研究一個投資標的：總體/技術/新聞/籌碼四角度並行蒐集，用 agy 深度研究交叉驗證，寫入分類研究筆記',
  phases: [
    { title: 'Gather', detail: '總體/技術/新聞/籌碼四個角度並行搜尋' },
    { title: 'DeepResearch', detail: '呼叫 agy 做交叉驗證的深度研究' },
    { title: 'Verify', detail: '比對 agy 與 WebSearch 結果，抓出衝突與錯誤' },
    { title: 'Synthesize', detail: '寫入 research/<slug>.md 並更新索引' },
  ],
}

let a = args
if (typeof a === 'string') {
  try { a = JSON.parse(a) } catch (e) { a = {} }
}
a = a || {}

// 專案根目錄：預設為使用者本機路徑，但允許呼叫端用 args.project_root 覆寫，
// 這樣在雲端/web session（路徑不同，例如 /home/user/stocks）也能正確寫入研究筆記。
const PROJECT_ROOT = a.project_root || '/var/www/docker/html/ks-devworks/stocks'

const target = a.target
if (!target) {
  log('缺少 args.target，無法執行研究')
  return { error: '缺少 args.target，呼叫時需提供 { target: "標的名稱", run_date: "YYYY-MM-DD" }', received_args_type: typeof args }
}

const slug = a.slug || String(target)
  .toLowerCase()
  .replace(/[^a-z0-9一-鿿]+/g, '-')
  .replace(/(^-+|-+$)/g, '')

const runDate = a.run_date || '（呼叫時未提供研究基準日期，請以查到的新聞時間戳為準）'
const focusQuestions = a.focus_questions || []
const angles = a.angles || [
  { key: '總體/政策面', prompt: '整理這個標的相關的總體經濟與央行/政策面最新狀況（例如利率、政策會議、法規變化），並附上你查到的資料來源網址。\n\n分析時請注意：(1) 不要只依單一數據點下結論，交叉比對至少兩個獨立來源；(2) 判斷一個數據是否代表趨勢，要比較 MoM 與 YoY，區分「一次性異常」跟「持續趨勢」；(3) 區分領先指標與落後指標，不要把落後指標（例如通膨數字）當領先訊號用；(4) 政策方向（例如升息vs降息、鷹派vs鴿派）判斷錯了會整個推翻結論，務必多方確認。' },
  { key: '技術面/價位', prompt: '整理這個標的目前的價格、近期走勢，以及市場上公認的關鍵支撐/阻力價位或技術分析觀點，並附上資料來源網址。\n\n除此之外，請務必明確查詢並回報以下五項技術指標的「最新數值與解讀」，缺一不可，結果請放進 technical_indicators 欄位：\n1. RSI（相對強弱指標，日線14日）：目前數值，以及是否處於超買（>70）或超賣（<30）區間。\n2. MACD（日線）：DIF/DEA數值或至少方向、目前是金叉還是死叉、柱狀圖是擴大還是收斂（動能增強或減弱）。\n3. 布林通道（Bollinger Bands，20日）：目前價格相對上軌/中軌/下軌的位置、通道正在收窄還是擴張（代表盤整或即將變盤）。\n4. 成交量趨勢：近期成交量是放大還是萎縮、有沒有異常爆量、跟價格走勢是背離還是同步。\n5. 波浪理論（Elliott Wave）：市場上公認的現況是處於哪一浪、哪個級別（degree，例如中期/短期），並務必附上這個計數的「失效價位」（invalidation level，價格一旦超過/跌破這個具體點位，此計數即作廢）；如果查到其他同樣站得住腳的替代計數（alternate count），也要一併列出，不要只給單一計數。\n每一項都要附上資料來源網址；如果某項指標真的查不到公開數據，在該項目明確寫「查無資料」，不要自己編造數字或用記憶回答。\n\n分析時請注意常見假訊號陷阱：(1) 強趨勢中RSI超買通常是動能確認而非反轉訊號，不要看到超買就直接判斷該反轉；(2) 布林通道觸及上/下軌在趨勢盤中是正常現象；(3) 盤整盤中MACD容易假交叉，需搭配零軸位置或價格結構判斷；(4) 若這五項指標訊號互相矛盾，要明確說明你採用的優先順序與理由，不要迴避矛盾直接給單一結論；(5) 務必嚴格區分「盤中高低點」跟「收盤價」，支撐阻力價位或走勢描述如果用的是盤中價，要註明清楚，不要跟收盤價混用；(6) 若這個標的天生波動度偏高（例如加密貨幣，波動度常是股票/傳統期貨的4-5倍），RSI超買超賣的70/30門檻與布林通道2倍標準差容易長時間停留在極端區而失去訊號意義，請改用較寬的門檻（例如RSI 80/20、布林2.5-3.0倍標準差）來解讀，並在回覆中註明你是否做了這個調整，不要直接套用股票的標準閾值判斷高波動資產；(7) 若是期貨商品且近期有換月/展倉，請註明近期成交量放大是否有相當比例來自技術性轉倉，避免把轉倉量誤讀為新增方向性部位；(8) 波浪理論高度主觀（學術批評如Aronson、Batchelor & Ramyar均質疑其可預測性與費波那契比例的統計顯著性），因此：只有三條「鐵律」是硬性規則、違反就直接判定計數錯誤——第2浪不可跌破第1浪起點、第3浪不可是第1/3/5浪中最短的一浪、第4浪不可與第1浪價格區間重疊（楔形除外）；除此之外的比例關係（例如第2浪回撤50-61.8%、第3浪延伸138.2-161.8%）只是經驗guideline不是必然成立的規則，不要當鐵律用；波浪理論只能當輔助/確認角度，不能單獨作為進場依據，必須跟RSI/MACD/布林通道/成交量交叉比對，方向一致才算加強訊號、方向矛盾要如實寫出來；若不同來源給出的浪數計數彼此不同，這正常反映此標的走勢當下不夠明朗，誠實呈現分歧、不要代為判定誰對；(9) ⚠ 資料源週期陷阱（2026-08-22 台指期與日圓同日各中一次）：技術指標頁（尤其 investing.com）標示的時間週期不一定是真的，已實際發生過標示「Daily」卻餵入小時線或 5 分鐘線資料的情況，導致 MACD 正負號與真實日線完全相反、ATR 差達 3.5 倍。引用任何一張技術指標表之前，請先做「自洽性檢查」：拿該頁的 ATR(14) 或古典樞紐點 S3→R3 的全距，去比對這個標的實際的「單日最高價減最低價」，若指標頁隱含的波動幅度明顯小於實際單日振幅（例如標示的 ATR 連一天的高低差都裝不下），就代表週期標示錯誤，該頁整張指標表都不可採用。遇到這種情況請在回覆中明確指出，並改用官方收盤價序列自行計算的數值、再找一個獨立平台（例如 Barchart）驗證吻合後才採用。' },
  { key: '新聞/催化事件', prompt: '整理這個標的最近的重大新聞、市場異動原因，以及未來已知的關鍵時間點/事件（例如財報、會議、到期日），並附上資料來源網址。\n\n分析時請注意：(1) 檢查這則消息公布前價格/成交量是否已提前反應（例如財報前已大漲可能代表「利多出盡」，公布當天追高容易套在高點）；(2) 如果消息內容跟市場既有預期一致（已被price in），要註明「已price in，催化效果有限」，不要直接當成新的方向性訊號來推導結論。' },
  { key: '籌碼/資金流向', prompt: '整理這個標的目前的籌碼/持倉/資金流向狀況（例如期貨COT報告、法人買賣超、選擇權未平倉），並附上資料來源網址。若這個標的沒有這類公開數據，請明確說明查不到，不要編造。\n\n分析時請注意：(1) 解讀期貨淨空單/淨多單時，務必先排除避險或delta中性策略的可能性（例如同時查現貨買賣超方向，若期貨放空但現貨同步大買，很可能是避險而非方向性看空），不要看到期貨淨空單增加就直接判斷「看空」；(2) COT報告資料通常有時間落差（例如美國落後至上週二收盤），請註明資料的實際時間點，不要當成即時部位解讀。' },
]

const FACT_SCHEMA = {
  type: 'object',
  properties: {
    claim: { type: 'string' },
    value: { type: 'string' },
    source_url: { type: 'string' },
  },
  required: ['claim'],
}

const GATHER_SCHEMA = {
  type: 'object',
  properties: {
    angle: { type: 'string' },
    summary: { type: 'string' },
    key_facts: { type: 'array', items: FACT_SCHEMA },
    key_price_levels: { type: 'array', items: { type: 'string' } },
    key_dates: { type: 'array', items: { type: 'string' } },
    technical_indicators: {
      type: 'object',
      description: '只有「技術面/價位」角度需要填寫；其他角度留空即可',
      properties: {
        rsi: { type: 'string' },
        macd: { type: 'string' },
        bollinger_bands: { type: 'string' },
        volume_trend: { type: 'string' },
        elliott_wave: { type: 'string', description: '目前浪位、級別、失效價位（invalidation level），以及替代計數（若有）' },
      },
    },
  },
  required: ['angle', 'summary', 'key_facts'],
}

const DEEPRESEARCH_SCHEMA = {
  type: 'object',
  properties: {
    agy_available: { type: 'boolean' },
    summary: { type: 'string' },
    key_facts: { type: 'array', items: FACT_SCHEMA },
  },
  required: ['agy_available', 'summary'],
}

const VERIFY_SCHEMA = {
  type: 'object',
  properties: {
    verified_facts: { type: 'array', items: { type: 'string' } },
    conflicting_facts: {
      type: 'array',
      items: {
        type: 'object',
        properties: {
          topic: { type: 'string' },
          note: { type: 'string' },
        },
        required: ['topic', 'note'],
      },
    },
    agy_errors: { type: 'array', items: { type: 'string' } },
  },
  required: ['verified_facts', 'conflicting_facts', 'agy_errors'],
}

phase('Gather')
log(`開始研究：${target}`)

const gatheredRaw = await parallel(angles.map(a => () =>
  agent(
    `你正在研究投資標的「${target}」。研究基準日期：${runDate}。\n\n${a.prompt}\n\n` +
    `請務必使用 WebSearch 工具查詢最新資訊，不要憑記憶回答。每一項事實都要附上來源網址；查不到就不要編造，直接說查無資料。`,
    { label: `gather:${a.key}`, phase: 'Gather', schema: GATHER_SCHEMA }
  )
))
const gatherResults = gatheredRaw.filter(Boolean)

phase('DeepResearch')
const priceAnchor = gatherResults
  .flatMap(g => g.key_price_levels || [])
  .slice(0, 6)
  .join('；')
const deepResearchRaw = await agent(
  `請對投資標的「${target}」執行深度研究（deep research）。研究基準日期：${runDate}。\n\n` +
  `作法：使用 Bash 工具呼叫 Antigravity CLI（指令是 agy），指令格式類似：\n` +
  `timeout 280 agy -p "<針對這個標的的總體面、價位、催化事件、進場時機的研究提示>" --output-format text --effort high\n\n` +
  `這個 <...> 裡實際下給 agy 的研究提示，內容除了總體面/價位/催化事件/進場時機之外，務必額外加上這段引註格式要求（逐字或同義即可）：「你回答中的每一個具體數字（點位、金額、口數、百分比等），都必須在該數字後面用括號附上你查到這個數字的來源網址與資料日期，格式例如：45,811點（來源：https://... ，2026-08-14）。如果某個數字你查不到具體來源網址，就不要給出精確數字，改用『概略』、『約略區間』或『不確定，需查證』這類措辭表達，不要用一份文末的通用網站清單取代逐項引註。」——這是因為過去測試發現，如果只在提示裡籠統要求「附上來源」，agy 會用一份跟具體數字沒有綁定關係的通用連結清單打發過去，必須明確要求「每個數字後面用括號附上專屬來源」才會真的改變它的輸出方式。\n\n` +
  `同時也要在這個 <...> 提示裡明確點名要求 agy 使用它自己內建、名為 search_web 的工具，例如加上這句（逐字或同義即可）：「請務必使用你的 search_web 工具實際查詢最新資料，不要只憑內部知識/訓練資料回答；每個具體數字都要能對應到一次真的執行過的搜尋。」——這是因為 search_web 是 agy 內建工具的正式名稱（來自 agy changelog），直接點名工具比籠統說「請上網查」更容易促使模型真的觸發它。\n\n` +
  `如果指令逾時、卡住、或出現要求登入（sign in）的錯誤訊息，代表 agy 目前無法使用，請直接回報 agy_available=false，並在 summary 說明「agy 未登入或逾時，本次跳過，請使用者執行 \`agy\` 完成登入」，不要重試、不要編造內容。\n\n` +
  `已知參考量級（來自本次已完成的 WebSearch 蒐集，僅供你事後檢查 agy 回傳數字的量級是否合理，不要直接把這些數字塞進你給 agy 的 prompt 裡、也不要暗示或誘導 agy 照抄這些值——目的是自我核對，不是提示答案）：${priceAnchor || '（無）'}\n` +
  `取得 agy 的回應後，請自行比對：如果 agy 給出的指數/價位/口數等數字，跟上面這個參考量級差距達到整數倍（例如相差約2倍、腰斬、翻倍），這通常代表 agy 抓到了過期或錯置的舊資料快照，而不是單純誤差，請在 summary 中明確註記「疑似整批套用舊資料快照，量級與已知參考值不符」，仍然把 agy 原始數字放進 key_facts（供後續 Verify 階段留存比對），但不要在 summary 中把這些數字當成可信結論帶出。\n\n` +
  `把 agy 回應整理成 key_facts 時，只有該數字緊跟著的括號裡有具體來源網址與資料時間點才收錄；文末另外列的一份通用網站/首頁清單（沒有逐項綁定到特定數字）不算數，不能拿來當作這個數字有來源。如果 agy 給的數字沒有這種緊跟在旁的專屬來源，這項就不要放進 key_facts，可以在 summary 中提及「agy 未附來源的說法：...」但標註為低可信度。\n` +
  `如果成功取得結果，把裡面具體的事實/數字整理成 key_facts，並在 summary 做整體摘要，設 agy_available=true。`,
  { label: 'agy-deep-research', phase: 'DeepResearch', schema: DEEPRESEARCH_SCHEMA }
)
const deepResearch = deepResearchRaw || { agy_available: false, summary: 'agy 呼叫失敗（agent無回應）', key_facts: [] }

phase('Verify')
const verify = await agent(
  `以下是針對投資標的「${target}」，從不同角度蒐集到的研究資料（JSON），以及另外用 agy 深度研究工具得到的結果（JSON）。\n\n` +
  `WebSearch蒐集結果：\n${JSON.stringify(gatherResults)}\n\n` +
  `agy深度研究結果：\n${JSON.stringify(deepResearch)}\n\n` +
  `請你交叉比對這些資料：\n` +
  `1. verified_facts：哪些事實在多個來源之間是一致的、可信的，列出簡短陳述。\n` +
  `2. conflicting_facts：哪些主題上，不同來源給出的數字/說法互相衝突或差異很大（例如持倉口數、機率預測），請列出主題與衝突內容，誠實呈現落差，不要幫忙猜一個「正確答案」。\n` +
  `3. agy_errors：agy深度研究結果中，有沒有跟WebSearch多方來源明顯矛盾、看起來是查錯或編造的具體事實（例如把利率數字記錯）。若agy_available=false或沒發現明顯錯誤，回傳空陣列即可，不要為了填內容硬找。\n\n` +
  `特別注意：根據過去多次研究的經驗（詳見 research/methodology.md），agy 常反覆出現以下幾種錯誤類型，請針對這幾種模式主動檢查，發現時要明確標記出來：\n` +
  `  (a) 把盤中最高/最低價誤植為收盤價\n` +
  `  (b) 日期記錯（例如會議日期、歷史高點日期）\n` +
  `  (c) 正負號或漲跌方向寫反（例如逆價差寫成正價差、或反過來）\n` +
  `  (d) 政策方向整個判斷相反（例如把「偏鷹/升息」講成「偏鴿/降息」）\n` +
  `  (e) 均線、RSI等指標數值大幅偏離其他來源、且方向也相反（不是單純誤差）\n` +
  `  (f) 編造查無其他來源佐證的具體數字\n` +
  `  (g) 引用過期或錯誤的舊聞當成最新資訊\n` +
  `  (h) 整批系統性偏移，疑似整包套用了過期或錯置的市場快照（跟單點誤植不同類）：檢查agy報告裡「彼此該互相對得上量級」的多個獨立欄位（例如指數點位、GDP、出口值、個股營收、期貨空單口數）是不是同步呈現同一個比例的落差（例如全部大約是實際值的一半或兩倍）。若是，代表很可能不是逐欄位各自查錯，而是整份報告套用了錯誤的資料快照，應建議這輪agy報告的具體數字整批不採信，而不是逐項排查。\n` +
  `  (i) 引用日內資料（小時線／5分鐘線）當成日線指標：檢查agy給的RSI、MACD、ATR、均線是否與其他來源的日線值出現「數值差距大且方向相反」的組合，尤其MACD正負號。若是，追查agy引用的來源頁面實際週期（自洽性檢查：該頁ATR或樞紐點全距若小於標的實際單日高低差，即為週期錯置），確認後該頁整張指標表都不採用，並提醒過去輪次若引用同一來源亦須一併作廢。\n` +
  `  (j) 波浪理論計數違反三條鐵律（例如宣稱的第3浪其實是第1/3/5浪中最短的一浪、或第2浪跌破第1浪起點、或第4浪與第1浪價格區間重疊卻未註明是楔形）：這種屬於可以客觀驗證對錯的規則違反，不是主觀計數分歧，發現時要明確標記為agy_errors，不要當成conflicting_facts處理。\n` +
  `不過也要注意：不能只因為「查無其他佐證」就直接斷定是幻覺，過去也有反例（例如某次美國Fed主席人選一開始被誤判為幻覺，後來查證後證實為真），如果無法確認，請如實在conflicting_facts中列出不確定性，不要武斷判定。`,
  { label: 'cross-verify', phase: 'Verify', schema: VERIFY_SCHEMA }
)

phase('Synthesize')
const notesPath = `${PROJECT_ROOT}/research/${slug}.md`
const indexPath = `${PROJECT_ROOT}/research/README.md`
const synthesis = await agent(
  `你要把投資標的「${target}」的研究結果整理成一份研究筆記檔案。研究基準日期：${runDate}。\n\n` +
  `背景資料：\n` +
  `1. 各角度蒐集結果（JSON）：${JSON.stringify(gatherResults)}\n` +
  `2. agy深度研究結果（JSON）：${JSON.stringify(deepResearch)}\n` +
  `3. 交叉驗證結果（JSON）：${JSON.stringify(verify)}\n` +
  (focusQuestions.length ? `4. 使用者特別想知道的問題：${JSON.stringify(focusQuestions)}\n` : '') +
  `\n請執行以下步驟：\n` +
  `1. 用 Read 工具檢查 ${notesPath} 是否已存在。如果已存在，讀取內容，把新研究結果併入更新（保留還有效的舊結論，更新過時或已被新資料推翻的部分），而不是整篇砍掉重寫；如果不存在則視為新建。\n` +
  `2. 用 Write 工具把最終研究筆記寫入 ${notesPath}，用繁體中文、清楚易懂的寫法（不要用英文），格式比照以下分類結構：\n` +
  `   # <標的名稱> 研究筆記\n` +
  `   最後更新：<研究基準日期>\n` +
  `   ## 現況快照\n` +
  `   ## 總體/政策面\n` +
  `   ## 技術關鍵價位（除了支撐/阻力價位表，也要有一個小節整理RSI、MACD、布林通道、成交量趨勢、波浪理論這五項指標目前的數值與解讀；波浪理論務必包含目前浪位/級別、失效價位、替代計數（若有）；查無資料的項目要註明「查無資料」）\n` +
  `   ## 籌碼/資金流向（查無公開資料要註明「查無公開資料」，不要編造）\n` +
  `   ## 進場策略框架（保守／結構確認型）\n` +
  `   列出偏多/偏空兩個方向各自的進場區間、停損、目標；如果目前價位卡在區間中段、沒有邊界確認訊號，要明確寫「目前不建議進場」並說明原因，不要為了給結論硬湊一個進場建議。\n` +
  `   下結論前，請依 research/book/16-building-your-own-system.md 的 checklist 邏輯自我檢查以下幾點，並把檢查結果的關鍵理由寫進這個小節（不用逐條列出checklist本身，但結論要能反映這些檢查）：\n` +
  `   (a) 四個角度（總體/技術/新聞/籌碼）是否互相印證？若資料齊全但彼此矛盾，這是最該迴避進場的情境，比「資料不足」更需要保守，要明確指出矛盾點；\n` +
  `   (b) 現價是否卡在支撐/阻力中段（兩邊距離都在1-2%以內）？若是，不建議進場；\n` +
  `   (c) 重大數據/會議是否在1-2天內？若是，不建議在事件前建立新的方向性部位；\n` +
  `   (d) 停損點的設定依據必須是價格結構或ATR等波動度指標，不要用RSI等動能指標的翻轉當停損觸發依據；\n` +
  `   (e) 若此商品有槓桿（期貨/保證金交易），提醒讀者進場前應自行換算清算價，確認觸及停損時部位會先出場、不會被強制平倉；\n` +
  `   (f) 不要建議「向下攤平/向上攤平」這種隨虧損擴大加碼的做法（近似馬丁格爾式加倍下注），如果要分批進場，應該是在停損/停利計畫內的預先設定分批，而不是虧損後臨時決定加碼攤平。\n` +
  `   最後，自我檢查一次：這個結論是不是只採信了支持某個方向的證據、卻沒有認真檢視反向訊號（確認偏誤）？如果有，回去補上被忽略的反向訊號再下結論。\n` +
  `   ## 動能策略觀點（順勢／動能型，對照用）\n` +
  `   這一小節是給採用動能/順勢交易風格（trend-following，不要求四角度全部印證、不等結構性突破確認）的讀者對照參考，判準與上面「保守／結構確認型」框架不同，請明確區分、不要混寫成同一套結論：\n` +
  `   - 用上面技術面角度蒐集到的RSI、MACD、均線方向、成交量趨勢，直接判斷目前動能方向是偏多/偏空/中性（例如「MACD零軸上金叉且柱狀圖擴大＋價格站上關鍵均線＝動能偏多訊號成立」），不需要等新聞面/籌碼面/總體面同步確認。\n` +
  `   - 若動能訊號方向明確，列出動能策略下的進場方式（例如順勢突破前波高/前波低、或站上/跌破特定均線），並明確區分兩種不同性質的出場機制，不要混寫成一句：(1) **硬性停損**（防止無限虧損的風控底線，一律依價格結構，例如跌破近期均線或前一根K棒低點，寫出具體點位）；(2) **動能反轉出場訊號**（趨勢轉弱時主動獲利了結或撤退，可依動能指標翻轉判斷，例如MACD死叉、RSI跌破50）——前者是「認錯離場」，後者是「趨勢可能已變、主動退出」，性質不同、不要用同一句話帶過。\n` +
  `   - 若技術指標本身互相矛盾或本輪出現跨來源大幅分歧（例如RSI不同來源差距超過10點），要明確寫「動能訊號本身不一致，此策略本輪不適用」，不要硬湊方向。\n` +
  `   - 結尾務必寫一段風險提醒：動能策略比結構確認型更早進場、也更早停損出場，換手更頻繁，在盤整或訊號矛盾時容易被連續小停損（whipsaw）打，且不像上面的框架會等事件公布或籌碼確認，本質上是用「更快反應」換「更高的假訊號風險」；這裡的停損也刻意省略了 research/book/01-risk-management-position-sizing.md 建議的「用ATR驗證停損距離是否≥1倍ATR」這道防雜訊步驟，是同一組取捨的一部分，讀者若採用動能框架應理解自己承擔的雜訊掃損風險高於結構確認型框架。兩套框架給出的方向可能不同，讀者需自行選擇適合自己風險偏好與交易頻率的一套，不是「動能策略比較積極所以比較對」。\n` +
  `   ## 反轉／逆勢策略觀點（Mean-Reversion，對照用）\n` +
  `   這一小節跟上面的動能小節是刻意相反的鏡像對照（依 research/book/18-investment-strategies-taxonomy.md 第8節），用同樣的技術面資料、給出可能相反的方向判斷，讓讀者同時看到「順勢」與「逆勢」兩種讀法：\n` +
  `   - 檢查現價是否處於統計極端：RSI是否遠離50（例如>75或<25，比動能小節判斷「方向」用的門檻更嚴格，因為逆勢交易要的是「過度反應」而非單純「有方向」）、價格是否觸及或穿出布林通道上下軌、是否較關鍵均線（如20日/60日線）偏離超過一段明顯距離。若現價只是溫和偏多/偏空、未達統計極端，直接寫「現價未達統計極端，反轉策略本輪不適用」，不要硬湊。\n` +
  `   - 若確實處於極端，給出反轉方向的進場方式（例如布林通道觸及上軌後價格重新收回軌內、RSI從極端區回落）、停損（設在剛剛觸及的極端點位之外，例如前波高/前波低）、目標（回歸至布林中軌或近期均線）。\n` +
  `   - 結尾務必寫一段風險提醒：逆勢交易在強趨勢中風險最高（俗稱「接落下的刀子」），依 research/book/18-investment-strategies-taxonomy.md 第8節，短期反轉與中期動能是不同時間窗口的不同機制、可能同時存在但方向相反，這一小節與上面動能小節若給出相反方向是正常現象、不是互相矛盾，讀者應理解自己在用哪一套時間窗口的邏輯下注，不要同時套用兩套邏輯又疊加部位。\n` +
  `   ## 利差交易觀點（Carry Trade，僅適用外匯／貨幣期貨類標的）\n` +
  `   這一小節只適用於外匯期貨、貨幣對這類「存在明確兩國利率差」的標的（依 research/book/18-investment-strategies-taxonomy.md 第9節）。**若研究標的不是外匯／貨幣期貨（例如股價指數期貨、個股、加密貨幣），直接寫「此策略不適用於此標的類型」，整節到此為止，不要勉強套用**：\n` +
  `   - 若適用，從上面總體/政策面角度蒐集到的兩國央行政策利率，判斷目前的利差方向與大致幅度，以及利差是正在擴大（有利於順著利差方向持有）還是收斂（提醒可能升溫的平倉風險）。\n` +
  `   - 說明這個策略的操作邏輯跟上面的進場框架不同：不是找一個價位突破就進出，而是「順著利差方向持有、賺取利差收益」，重點風險不是價格突破失敗，而是利差交易特有的**負偏態尾部風險**（平時穩定小賺、一旦避險情緒急升就可能瞬間虧掉數月/數年累積的利差收益，2024年8月日圓利差交易unwind是代表案例）。\n` +
  `   - 明確列出本輪已知、可能觸發利差交易平倉潮的風險事件（例如兩國央行下次會議日期、利率決策），並提醒讀者若要採用這個策略，部位規模應該比方向性突破交易更保守，因為尾部風險發生時的跳空幅度可能遠大於一般停損能防住的範圍。\n` +
  `   ## 風險管理備註\n` +
  `   給一個簡短的部位規模試算範例（依 research/book/01-risk-management-position-sizing.md 的固定比例法或ATR法擇一示範即可，例如「假設帳戶風險上限設為單筆1%，停損距離X點/元，對應部位約Y口/股」），並註明這只是示範公式、不是資金建議，實際部位大小需讀者依自己帳戶規模與風險承受度調整。\n` +
  `   ## 待觀察催化事件\n` +
  `   ## 分析師分歧觀點\n` +
  `   ## 資料衝突與查證備註（把 verify 階段的 conflicting_facts 跟 agy_errors 誠實列出來，不要隱藏）\n` +
  `3. 用 Read 工具讀取 ${indexPath}（如果不存在就視為要新建一份索引，格式比照：一個「進行中」表格，欄位是 標的/檔案/狀態/最後更新，以及一個「待研究」區塊）。用 Edit 或 Write 工具更新或新增這個標的在索引表格中的那一列（狀態欄位要精簡摘要目前是否建議進場）。\n` +
  `4. 最後，回傳一段給使用者看的中文摘要（5-8句話），講清楚：現在的結論是什麼、建不建議進場、下一個該觀察的價位或事件是什麼。**若結論是不建議進場，不要只停在「不建議進場」就結束**——務必從「進場策略框架」小節裡，把偏多與偏空兩個方向各自「需要看到什麼價位/什麼條件成立才會考慮進場」的具體觸發點各自濃縮成一句話帶出來（例如「若帶量站穩X之上可考慮偏多，進場區約X-Y、停損Z；若跌破X且收盤確認可考慮偏空，進場區約X-Y、停損Z」），讓使用者不用回去翻整份筆記就知道要盯緊哪些具體數字，而不是只停留在方向與是否建議進場。`,
  { label: 'write-notes', phase: 'Synthesize' }
)

log('研究筆記已寫入並更新索引')
return {
  target,
  slug,
  notesPath,
  agy_available: deepResearch.agy_available,
  verify,
  summary: synthesis,
}
